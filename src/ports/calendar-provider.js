/**
 * CalendarProvider port
 *
 * getAvailability(date, calendarConfig) → [{ start, end }]
 * bookAppointment(appointment, calendarConfig) → { id, htmlLink, hangoutLink }
 * getScheduledAppointments(email, calendarConfig) → events[]
 * cancelAppointment(eventId, calendarConfig) → void
 * checkHealth(calendarConfig) → { available }
 */
