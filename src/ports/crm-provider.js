/**
 * CRMProvider port
 *
 * getOrCreateContact(email, properties) → contact
 * createDeal(contactId, dealName, amount, properties) → deal
 * getAuthorizationUrl() → string
 * exchangeAuthorizationCode(code) → tokens
 */
